int testFunc(int[] array) {
    int t;
    if (array.length > 0) {
        t = array.length + 1;
        println(t);
    } else {
        print(t);
    }

    println(t);

    return array.length;
}

void main() {
    print("Hello, ");
    print(name);
    print(exp(2, 8));
    println("!");

    float number;
    float anotherNumber = 2.12;

    if (number == anotherNumber)
        print(number);

    int[] myArr;
    testFunc(myArr);
}