int testFunc(int[] array, char[] array) {
    float[] array = {1.0, 5.4, 2.5, 3.3};
    int testFunc = array.length;

    return testFunc;
}

void main() {
    int sum = a + 231;
    print (sum);

    bool val = test(21.2);

    while (bFalse) {
        float[] var = new float[sum];
        val = true;

        if (val) {
            int c = testFunc({1,2,3});
            int sum = 2;
            float val = 3.4;
            println(sum + c);
        }

        println(sum + c);
    }
}