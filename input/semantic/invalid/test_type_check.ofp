int getInt() {
    return 1.224;
}

void main() {
    print("Hello, ");
    print(name);
    print(exp(2, 8));
    println("!");

    int number;
    print(number);
    print(result);
    number = 2;
    float result;
    result = 1.3;
    bool boolExpr;
    println(boolExpr);
    boolExpr = 12.2 + number + getInt() == result;
    boolExpr = getInt() * 3.4 / 3.8 == number * 3.3 - 3.0;

    int firstParameter = 12;
    float floatValue = func(firstParameter, true, getInt(43));
    char c = 'a';

    if (number.length == c) {
        print("Wrong");
    }
    if (4.5 < c) {
        print("Wrong");
    }

    float index = 2.2;
    string s = "Hello";
    char firstChar = s[index];
    s[index] = 'd';

    char[] arr = {'a', 'A'};
    int condition = 1;
    if (condition)
        print(arr);
    arr[condition] = 'f';
    arr[floatValue] = 'l';
    print(arr[floatValue]);
    println(arr[condition]);
}

int func(float f, bool b, int i, float[] floatArr) {
    return i;
}